import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  container: {
    flex: 1,
    marginHorizontal: 4,
    marginTop: -30,
    paddingBottom: 20,
  },
});

export default style;